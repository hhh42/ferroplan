---
name: compose
description: Compose a new Claude Code operating capability from admitted phase dimensions, agents, skills, hooks, MCP authorities, ownership law, and validation rails. Use when a fixed workflow is insufficient.
context: fork
agent: chatman-ecosystem:config-law-architect
effort: max
---

Compose a capability for `$ARGUMENTS` using design for combinatorial maximalism.

1. Enumerate the smallest orthogonal primitives already present.
2. Read `profiles/claude-projection.json`, `profiles/phase-space.json`, `profiles/artifact-ownership.json`, and `ontology/authority-graph.ttl`.
3. Select a lawful product-state combination and compute its capability, agent, and skill union.
4. Identify missing primitives instead of inventing a monolithic workflow.
5. Express missing law in canonical owners: ontology, SHACL, authority graph, phase profile, PDDL operator, MCP schema, or validation fixture.
6. Determine every projection that ggen or the bounded manufacturer must regenerate.
7. Validate the proposed topology with the source projection validator, current Claude loader when available, and explicit config-LSP analysis over modeled surfaces.
8. Preserve reversibility: every composition must decompose into independently inspectable components.
9. Preserve authority: composition cannot raise any component above its claim ceiling or create a second source actuator.
10. Preserve bounded recursion: recursive CMCA frontiers remain exactly eight nodes and bind their parent receipt.

Return the selected combination, rejected combinations, new primitive requirements, owner changes, generated projections, validation obligations, and maximum lawful standing.

Do not edit in this skill. Route admitted construction to `source-manufacturer`.
